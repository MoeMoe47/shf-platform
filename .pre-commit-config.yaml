repos:
  - repo: local
    hooks:
      - id: shf-registry-guard
        name: SHF Registry Guard (no legacy registry paths)
        entry: tools/precommit/check_registry_guard.py
        language: system
        pass_filenames: false
