// src/hooks/usePrefetchOnHover.js
import { useCallback } from "react";

/**
 * usePrefetchOnHover
 * Stubbed hook to prevent build errors.
 * Later we can wire this into React Router's prefetch or
 * custom data preloading logic.
 */
export default function usePrefetchOnHover() {
  return useCallback(() => {
    // 🚀 Future: add prefetch logic here
    // Example: router.prefetch(href) or fetch JSON
  }, []);
}
