export default function DecisionBand({ summary }) {
  return (
    <div
      style={{
        marginTop: 12,
        padding: "18px 22px",
        borderRadius: 14,
        border: "1px solid rgba(34,197,94,0.35)",
        background: "linear-gradient(180deg, rgba(2,6,23,0.95), rgba(2,6,23,0.85))",
        color: "#e2e8f0",
        fontSize: 18,
        fontWeight: 600,
        letterSpacing: 0.3,
        boxShadow: "0 0 0 1px rgba(34,197,94,0.08)"
      }}
    >
      {summary}
    </div>
  );
}
