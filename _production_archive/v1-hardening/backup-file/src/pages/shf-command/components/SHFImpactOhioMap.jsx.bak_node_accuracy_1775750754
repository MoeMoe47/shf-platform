import React, { useState } from "react";
import "./shf-impact-ohio-map.css";

const POINTS = [
  { name: "Franklin", x: 46.5, y: 69.0 },
  { name: "Cuyahoga", x: 73.5, y: 30.0 },
  { name: "Hamilton", x: 30.5, y: 91.0 },
  { name: "Summit", x: 67.0, y: 46.5 },
  { name: "Montgomery", x: 37.5, y: 81.5 },
  { name: "Lucas", x: 75.0, y: 10.5 },
];

export default function SHFImpactOhioMap() {
  const [hovered, setHovered] = useState(null);

  return (
    <div className="shf-impact-map">

      {/* TEXTURE */}
      <img
        src="/assets/shf-command/maps/shf-impact-ohio-texture.png"
        className="shf-map-texture"
      />

      {/* COUNTY LINES */}
      <img
        src="/assets/shf-command/maps/shf-impact-ohio-counties.png"
        className="shf-map-counties"
      />

      {/* NODES */}
      <div className="shf-map-nodes">
        {POINTS.map(p => (
          <div
            key={p.name}
            className="shf-node"
            style={{ left: `${p.x}%`, top: `${p.y}%` }}
            onMouseEnter={() => setHovered(p.name)}
            onMouseLeave={() => setHovered(null)}
          />
        ))}
      </div>

      {hovered && (
        <div className="shf-tooltip">
          <strong>{hovered} County</strong>
        </div>
      )}

    </div>
  );
}
