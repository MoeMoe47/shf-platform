# SHS BOS Package H Batch 01 Kernel Dependency Graph

The kernel depends only on Python standard library modules and sibling modules inside `services.extension_kernel`.

No owner dependencies, runtime dependencies, router dependencies, framework dependencies, or frontend dependencies are introduced.
