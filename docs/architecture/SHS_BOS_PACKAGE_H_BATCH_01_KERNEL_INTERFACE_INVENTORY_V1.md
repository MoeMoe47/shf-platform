# SHS BOS Package H Batch 01 Kernel Interface Inventory

The interface inventory includes registry, manifest validation, health, diagnostics, approval, activation request, event catalog, configuration, and generic capability protocols.

Capability protocols cover read, write, event definition, projection, notification, validation, observer, adapter, hook, middleware, plugin, and registry contracts.
