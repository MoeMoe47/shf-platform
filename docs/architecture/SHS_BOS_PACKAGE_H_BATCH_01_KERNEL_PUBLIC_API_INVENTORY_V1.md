# SHS BOS Package H Batch 01 Kernel Public API Inventory

The public API is exposed through `services.extension_kernel` and includes constants, models, interfaces, registry abstractions, lifecycle helpers, validators, versioning helpers, event definitions, diagnostics helpers, serialization helpers, errors, and utilities.
