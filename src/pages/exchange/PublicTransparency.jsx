import React from "react";

export default function PublicTransparency() {
  return (
    <section
      style={{
        background: "#111827",
        border: "1px solid rgba(255,255,255,0.08)",
        borderRadius: 16,
        padding: 20,
      }}
    >
      <h2 style={{ marginTop: 0 }}>Public Transparency</h2>
      <p style={{ color: "#cbd5e1", lineHeight: 1.6, marginBottom: 0 }}>
        This surface will expose public trust metrics, transparency ledger views,
        aggregate impact, and verified outcome visibility without exposing restricted data.
      </p>
    </section>
  );
}
