import React from "react";
import { HUB_BUSINESS_NAV_ITEMS as navItems, HUB_ORBITER_LOGO } from "./shared/hubBusinessNetworkNav.js";
import {
  hubPartners,
  hubOpportunities,
  hubBundles,
} from "./shared/hubBusinessNetworkData.js";
import {
  money,
  buildRelationshipMatches,
  scoreBundle,
  getPartnerName as getSharedPartnerName,
  explainRelationship,
  explainBundle,
} from "./shared/hubBusinessNetworkScoring.js";
import "./hub-intelligence-page.css";
import HubBusinessTourProvider from "./shared/HubBusinessTourProvider.jsx";
import {
  readHubEvents,
  addHubEvent,
} from "./shared/hubBusinessNetworkStorage.js";

const localPartnersDeprecated = [
  {
    id: "partner_workforce_001",
    name: "Franklin Workforce Partner",
    type: "Workforce Provider",
    sector: "workforce",
    serviceLanes: ["training", "job-placement", "case-support"],
    geography: ["Franklin County", "Central Ohio"],
    capacity: 82,
    responseSpeed: 91,
    completionRate: 88,
    verifiedOutcomeRate: 84,
    pricingTier: "competitive",
    trustScore: 89,
    businessNeeds: ["employer partners", "transportation partners"],
    offers: ["training", "placement", "workforce reporting"],
    idealMatches: ["staffing", "transportation", "case-management"],
  },
  {
    id: "partner_transport_001",
    name: "Central Mobility Services",
    type: "Transportation Partner",
    sector: "transportation",
    serviceLanes: ["transportation", "ride-coordination"],
    geography: ["Franklin County"],
    capacity: 68,
    responseSpeed: 86,
    completionRate: 81,
    verifiedOutcomeRate: 74,
    pricingTier: "competitive",
    trustScore: 82,
    businessNeeds: ["workforce programs", "healthcare programs"],
    offers: ["scheduled rides", "route support", "client transport"],
    idealMatches: ["workforce", "healthcare", "recovery"],
  },
  {
    id: "partner_staffing_001",
    name: "Central Staffing Group",
    type: "Staffing Agency",
    sector: "staffing",
    serviceLanes: ["employment", "placement", "employer-services"],
    geography: ["Central Ohio"],
    capacity: 76,
    responseSpeed: 89,
    completionRate: 84,
    verifiedOutcomeRate: 79,
    pricingTier: "premium",
    trustScore: 86,
    businessNeeds: ["trained workers", "credential programs"],
    offers: ["job placement", "employer relationships", "screening"],
    idealMatches: ["workforce", "training", "transportation"],
  },
  {
    id: "partner_recovery_001",
    name: "Kermit Recovery Support",
    type: "Recovery Support Program",
    sector: "recovery",
    serviceLanes: ["recovery-support", "case-support", "peer-support"],
    geography: ["Franklin County"],
    capacity: 71,
    responseSpeed: 78,
    completionRate: 80,
    verifiedOutcomeRate: 77,
    pricingTier: "standard",
    trustScore: 83,
    businessNeeds: ["housing partners", "transportation partners", "employment partners"],
    offers: ["peer support", "recovery navigation", "case support"],
    idealMatches: ["housing", "transportation", "workforce"],
  },
];

const localOpportunitiesDeprecated = [
  {
    id: "opp_001",
    title: "County Reentry Workforce Proposal",
    type: "Bundled Opportunity",
    estimatedValue: 125000,
    urgency: "High",
    deadlineDays: 30,
    neededLanes: ["training", "transportation", "case-support", "job-placement", "reporting"],
    geography: "Franklin County",
    status: "Open",
  },
  {
    id: "opp_002",
    title: "Healthcare Workforce Transportation Support",
    type: "Cross-Sale Opportunity",
    estimatedValue: 42000,
    urgency: "Medium",
    deadlineDays: 45,
    neededLanes: ["transportation", "workforce", "healthcare-support"],
    geography: "Central Ohio",
    status: "Suggested Match",
  },
];

const localBundlesDeprecated = [
  {
    id: "bundle_001",
    name: "Franklin Workforce Reentry Bundle",
    opportunityId: "opp_001",
    partners: [
      "partner_workforce_001",
      "partner_transport_001",
      "partner_staffing_001",
      "partner_recovery_001",
    ],
    requiredLanes: ["training", "transportation", "case-support", "job-placement", "reporting"],
    pricingStrategy: "competitive",
    estimatedPrice: 118000,
    estimatedValue: 125000,
    status: "Recommended",
  },
  {
    id: "bundle_002",
    name: "Lowest-Cost Workforce Support Bundle",
    opportunityId: "opp_001",
    partners: ["partner_workforce_001", "partner_transport_001"],
    requiredLanes: ["training", "transportation", "job-placement"],
    pricingStrategy: "low-cost",
    estimatedPrice: 92000,
    estimatedValue: 125000,
    status: "Alternative",
  },
];


const partners = hubPartners;
const opportunities = hubOpportunities;
const bundles = hubBundles;

function go(hash) {
  window.location.hash = hash;
}



function getPartnerName(id) {
  return getSharedPartnerName(partners, id);
}









export default function HubIntelligencePage() {
  const [feedback, setFeedback] = React.useState(() => readHubEvents("intelligence"));
  const matches = React.useMemo(() => buildRelationshipMatches(partners), []);

  const scoredBundles = React.useMemo(() => {
    return bundles
      .map((bundle) => {
        const opportunity =
          opportunities.find((item) => item.id === bundle.opportunityId) || opportunities[0];

        return {
          ...bundle,
          opportunity,
          score: scoreBundle(bundle, opportunity, partners),
        };
      })
      .sort((a, b) => b.score.fitScore - a.score.fitScore);
  }, []);

  const topBundle = scoredBundles[0];

  const recordFeedback = (type, target) => {
    const { events } = addHubEvent("intelligence", type, { target }, 75);
    setFeedback(events);
  };

  return (
    <HubBusinessTourProvider pageKey="intelligence">
      <main className="hin-shell">
      <aside className="hin-rail">
        <button className="hin-logo" onClick={() => go("#/hub")} aria-label="Go to SHS Hub">
          <img src={HUB_ORBITER_LOGO} alt="SHS" onError={(event) => { event.currentTarget.style.display = "none"; }} />
          <span>SHS</span>
        </button>

        <nav className="hin-nav" aria-label="Hub navigation">
          {navItems.map(([icon, label, hash]) => (
            <button
              key={label}
              className={label === "Intel" ? "is-active" : ""}
              onClick={() => go(hash)}
            >
              <span>{icon}</span>
              <small>{label}</small>
            </button>
          ))}
        </nav>

        <div className="hin-readiness">
          <strong>INTEL</strong>
          <div>94%</div>
          <span>Active</span>
        </div>
      </aside>

      <section className="hin-page">
        <header className="hin-header" data-tour="hub-intel-hero">
          <div>
            <p>SHS HUB INTELLIGENCE ENGINE</p>
            <h1>Hub Intelligence</h1>
            <span>
              Explainable recommendation intelligence for partner matches, bundled
              opportunities, pricing, delivery readiness, risk, and network growth.
            </span>
            <div className="hin-headerNote">
              V1 uses explainable simulated scoring from mock partner, opportunity,
              pricing, and performance data. It guides operator review and creates a
              feedback loop before deeper machine learning is introduced.
            </div>
          </div>

          <div className="hin-actions">
            <button onClick={() => go("#/hub/growth-network")}>Growth Network</button>
            <button onClick={() => go("#/hub/bundles")}>Bundle Builder</button>
            <button className="is-primary" onClick={() => recordFeedback("intel_review_started", "hub_intelligence")}>
              Start Review
            </button>
          </div>
        </header>

        <section className="hin-kpiStrip" data-tour="hub-intel-kpis">
          <article className="hin-kpi">
            <span>◉</span>
            <div>
              <h3>Recommendations</h3>
              <strong>{matches.length + scoredBundles.length}</strong>
              <p>Generated by V1 scoring</p>
            </div>
          </article>

          <article className="hin-kpi">
            <span>✓</span>
            <div>
              <h3>Accepted</h3>
              <strong>{feedback.filter((item) => item.type.includes("accepted")).length}</strong>
              <p>Operator-approved actions</p>
            </div>
          </article>

          <article className="hin-kpi">
            <span>▦</span>
            <div>
              <h3>Bundle Suggestions</h3>
              <strong>{scoredBundles.length}</strong>
              <p>Compared by fit and risk</p>
            </div>
          </article>

          <article className="hin-kpi">
            <span>$</span>
            <div>
              <h3>Pricing Flags</h3>
              <strong>2</strong>
              <p>Estimated pricing signals</p>
            </div>
          </article>

          <article className="hin-kpi">
            <span>!</span>
            <div>
              <h3>Risk Alerts</h3>
              <strong>{scoredBundles.filter((item) => item.score.riskLevel !== "Low").length}</strong>
              <p>Needs operator review</p>
            </div>
          </article>

          <article className="hin-kpi">
            <span>↻</span>
            <div>
              <h3>Feedback Events</h3>
              <strong>{feedback.length}</strong>
              <p>Learning loop activity</p>
            </div>
          </article>
        </section>

        <section className="hin-grid">
          <div className="hin-main">
            <article className="hin-panel hin-recommendation" data-tour="hub-intel-recommendation">
              <div className="hin-panelHead">
                <div>
                  <span className="hin-chip hin-chip--cyan">Top Recommendation</span>
                  <h2>{topBundle.name}</h2>
                  <small>V1 estimated bundle recommendation</small>
                </div>
                <strong>{topBundle.score.fitScore}% Fit</strong>
              </div>

              <p>{explainBundle(topBundle, topBundle.score)}</p>

              <div className="hin-scoreGrid">
                <div>
                  <span>Service Coverage</span>
                  <strong>{topBundle.score.serviceCoverage}%</strong>
                </div>
                <div>
                  <span>Pricing Fit</span>
                  <strong>{topBundle.score.pricingCompetitiveness}%</strong>
                </div>
                <div>
                  <span>Readiness</span>
                  <strong>{topBundle.score.deliveryReadiness}%</strong>
                </div>
                <div>
                  <span>Outcomes</span>
                  <strong>{topBundle.score.outcomeConfidence}%</strong>
                </div>
                <div>
                  <span>Success Probability</span>
                  <strong>{topBundle.score.successProbability}%</strong>
                </div>
                <div>
                  <span>Risk</span>
                  <strong className={`risk-${topBundle.score.riskLevel.toLowerCase()}`}>
                    {topBundle.score.riskLevel}
                  </strong>
                </div>
              </div>

              <div className="hin-partnerLine">
                {topBundle.partners.map((id) => (
                  <span key={id}>{getPartnerName(id)}</span>
                ))}
              </div>

              <div className="hin-modelNote">
                Pricing and success scores are simulated V1 intelligence signals.
                Use them for review, comparison, and planning — not final pricing decisions.
              </div>

              <div className="hin-panelActions">
                <button onClick={() => recordFeedback("accepted_bundle", topBundle.id)}>
                  Accept Recommendation
                </button>
                <button onClick={() => recordFeedback("meeting_scheduled", topBundle.id)}>
                  Mark Meeting Scheduled
                </button>
                <button onClick={() => recordFeedback("proposal_created", topBundle.id)}>
                  Mark Proposal Created
                </button>
                <button onClick={() => recordFeedback("rejected_bundle", topBundle.id)}>
                  Reject
                </button>
              </div>
            </article>

            <article className="hin-panel">
              <div className="hin-panelHead">
                <div>
                  <span className="hin-chip hin-chip--gold">Recommended Matches</span>
                  <h2>Partner Relationship Intelligence</h2>
                </div>
              </div>

              <div className="hin-matchList">
                {matches.map((match) => (
                  <article className="hin-matchCard" key={match.id}>
                    <div>
                      <h3>
                        {match.partnerA.name} <span>↔</span> {match.partnerB.name}
                      </h3>
                      <p>{explainRelationship(match)}</p>
                    </div>

                    <div className="hin-matchMetrics">
                      <strong>{match.score}%</strong>
                      <small>Match</small>
                    </div>

                    <div className="hin-matchMetrics">
                      <strong>{match.crossSalePotential}%</strong>
                      <small>Cross-Sale</small>
                    </div>

                    <div className={`hin-riskPill risk-${match.riskLevel.toLowerCase()}`}>
                      {match.riskLevel}
                    </div>

                    <button onClick={() => recordFeedback("intro_made", match.id)}>
                      Introduce
                    </button>
                  </article>
                ))}
              </div>
            </article>

            <article className="hin-panel">
              <div className="hin-panelHead">
                <div>
                  <span className="hin-chip hin-chip--blue">Bundle Comparison</span>
                  <h2>Recommended Bundles</h2>
                </div>
              </div>

              <div className="hin-bundleTable">
                <div className="hin-tableHead">
                  <span>Bundle</span>
                  <span>Fit</span>
                  <span>Pricing</span>
                  <span>Readiness</span>
                  <span>Risk</span>
                </div>

                {scoredBundles.map((bundle) => (
                  <div className="hin-tableRow" key={bundle.id}>
                    <span>
                      <strong>{bundle.name}</strong>
                      <small>{money(bundle.estimatedPrice)} estimated price</small>
                    </span>
                    <b>{bundle.score.fitScore}%</b>
                    <b>{bundle.score.pricingCompetitiveness}%</b>
                    <b>{bundle.score.deliveryReadiness}%</b>
                    <em className={`risk-${bundle.score.riskLevel.toLowerCase()}`}>
                      {bundle.score.riskLevel}
                    </em>
                  </div>
                ))}
              </div>
            </article>
          </div>

          <aside className="hin-side">
            <article className="hin-panel">
              <div className="hin-panelHead">
                <div>
                  <span className="hin-chip hin-chip--green">Explanation</span>
                  <h2>Why This Works</h2>
                </div>
              </div>

              <ul className="hin-explainList">
                <li>Strong service coverage across required opportunity lanes.</li>
                <li>Partner response and completion history support delivery readiness.</li>
                <li>Estimated pricing is competitive against opportunity value.</li>
                <li>Outcome confidence is based on verified outcome rate signals.</li>
                <li>Risk is low enough to move into proposal planning with review.</li>
              </ul>
            </article>

            <article className="hin-panel">
              <div className="hin-panelHead">
                <div>
                  <span className="hin-chip hin-chip--violet" data-tour="hub-intel-feedback">Feedback Logger</span>
                  <h2>Adaptive Learning</h2>
                </div>
                <strong>{feedback.length}</strong>
              </div>

              <div className="hin-feedbackActions">
                <button onClick={() => recordFeedback("accepted_recommendation", "general")}>Accept</button>
                <button onClick={() => recordFeedback("rejected_recommendation", "general")}>Reject</button>
                <button onClick={() => recordFeedback("intro_made", "general")}>Intro Made</button>
                <button onClick={() => recordFeedback("proposal_won", "general")}>Won</button>
                <button onClick={() => recordFeedback("proposal_lost", "general")}>Lost</button>
                <button onClick={() => recordFeedback("outcome_verified", "general")}>Outcome Verified</button>
              </div>

              <div className="hin-feedbackLog">
                {feedback.length ? (
                  feedback.slice(0, 6).map((event) => (
                    <div key={event.id}>
                      <span>{event.type.replaceAll("_", " ")}</span>
                      <small>{new Date(event.createdAt).toLocaleString()}</small>
                    </div>
                  ))
                ) : (
                  <p>No feedback logged yet. Start review to begin the learning loop.</p>
                )}
              </div>
            </article>

            <article className="hin-panel">
              <div className="hin-panelHead">
                <div>
                  <span className="hin-chip hin-chip--orange">Next Action</span>
                  <h2>Operator Move</h2>
                </div>
              </div>

              <p>
                Prepare a joint proposal review for the Franklin Workforce Reentry
                Bundle. Confirm partner capacity, final pricing, scope, and reporting role.
              </p>

              <div className="hin-panelActions">
                <button onClick={() => go("#/hub/bundles")}>Open Bundle Builder</button>
              </div>
            </article>
          </aside>
        </section>

        <footer className="hin-footer">
          <div>
            <strong>Silicon Heartland Solutions</strong>
            <span>Hub Intelligence Engine · Explainable Adaptive Scoring · Match & Bundle Recommendations</span>
          </div>

          <p>
            Powered by Silicon Heartland Solutions — intelligent coordination, verified collaboration, and network growth.
          </p>
        </footer>
      </section>
      </main>
    </HubBusinessTourProvider>
  );
}
